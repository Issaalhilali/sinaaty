# CLAUDE.md — Sinaaty (صناعية)

Guidance for Claude Code when working in this repository. Read this fully before touching code.

## 1. What this project is

**Sinaaty (صناعية)** is a three-sided platform for the Saudi automotive service sector: customers & fleets ↔ workshops/factories ↔ parts dealers/scrapyards, plus tow trucks/logistics. Its core differentiator is the **legal + financial layer**:

- Work orders are digitally signed by the customer via **Nafath (نفاذ)**.
- Deferred/unpaid amounts become an **electronic promissory note (سند لأمر)** issued through **Nafez (نافذ)**, enforceable via **Najiz (ناجز)**.
- Payments (Mada / Apple Pay) go through a licensed PSP into an **escrow hold**; funds are released to the provider only after delivery/confirmation.
- Paying the invoice **automatically closes the note** and issues a digital **settlement/clearance (مخالصة)**.
- Invoices are **ZATCA** compliant (Phase 1 now, Phase 2 Fatoora in P3).
- Parts are sourced by **VIN + reverse auction** and, from **parts distributors/brand agents**, via a **VIN-fitment catalog with live inventory (Buy Now)**; workshops buy from distributors on a **Nafez-secured trade account** (every deferred invoice = promissory note); genuine parts carry a **QR serial (anti-counterfeit)** and used/new parts carry a **digital warranty** (part + labor).

Authoritative documents (read the relevant one before implementing a feature):

| Doc | Purpose |
|---|---|
| `docs/01-PRD.md` | Product requirements, personas, FR/NFR IDs (e.g. `FR-PAY-04`), phases |
| `docs/02-ARCHITECTURE.md` | Modules, sequence diagrams, state machines, integrations, API map |
| `docs/03-TECH-STACK.md` | Chosen technologies, packages, monorepo layout |
| `docs/04-DATABASE.md` + `docs/db/schema.sql` | Data model (79 tables). `schema.sql` is the human source of truth; Prisma migrations must match it |
| `docs/06-DASHBOARDS.md` | Dashboard specs: admin back-office (roles, screens, KPIs, critical screens), workshop dashboard, distributor/scrapyard dashboards, fleet portal. Build Step 14/15/22/26 UIs from it |
| `docs/05-USER-FLOWS.md` | Day-in-the-life flows, screen maps, edge cases per party (workshop, customer, parts distributor, scrapyard, driver, admin). Build Flutter screens from these maps; turn edge cases into acceptance tests |
| `docs/adr/` | Architecture Decision Records — add one for every non-trivial decision |

When PRD/Architecture and code disagree, **stop and ask** (or write an ADR) — do not silently diverge.

## 2. Repository layout (target monorepo)

```
sinaaty/
├── CLAUDE.md
├── docs/                      # PRD, architecture, DB, ADRs
├── apps/
│   ├── mobile/                # Flutter — customer | partner | fleet flavors  (currently the root Flutter template; see §8 step 0)
│   ├── api/                   # NestJS modular monolith (REST + WebSocket + BullMQ workers)
│   └── admin-web/             # Next.js back-office
├── packages/
│   ├── shared-types/          # TS enums/DTOs; OpenAPI → Dart codegen source
│   ├── zatca-ubl/             # UBL 2.1 XML + XAdES + QR TLV
│   ├── ledger/                # double-entry posting helpers
│   └── ui-tokens/             # design tokens (colors, spacing, Arabic fonts)
├── infra/                     # docker-compose, terraform, helm
└── .github/workflows/
```

**Current state (2026-08-17, after Step 2):** monorepo in place (Step 0); `apps/api` scaffolded — NestJS 11, Clean Architecture layers with lint-enforced dependency rule, `/v1/health`, Swagger `/docs`, bilingual error envelope, pino, throttler (Step 1); Prisma schema with 79 models generated from `docs/db/schema.sql` via the init migration, idempotent seed, drift check (Step 2); shared kernel — `packages/shared-types` (46 enums generated from schema.prisma, `gen:check` in CI), `packages/ledger` (balanced entries, chart of accounts, posting templates, 11 tests incl. property test), `apps/api/src/common/` (Money, VAT per-line, UUIDv7, AES-256-GCM `PiiCryptoService`, PII redaction, `AuditLogWriter` hash chain + advisory lock, `OutboxWriter`) all in `CommonModule` (Step 3); identity module — Nafath port + mock (auto-approve, forced states via callback), OTP (6-digit, hashed, 3/10min, 5 attempts), EdDSA access JWT (15 min) + opaque refresh with rotation & family reuse detection, devices, global `JwtAuthGuard` (default deny, `@Public()`), `RolesGuard` (`@Roles({platform, org, nafath})`), `@CurrentUser()`, `assertOwnership()`; 16 e2e tests (Step 4); `UnitOfWork` port (`UNIT_OF_WORK` → `PrismaUnitOfWork`, opaque `TxHandle` accepted by repositories/`AuditLogWriter`/`OutboxWriter`); media module (`POST /v1/media/presign` → `media_assets` + `ObjectStoragePort` mock); organizations module — create/update, org status state machine via `OrgTransitionService` (audit + outbox in one tx), members by phone with per-type roles, PostGIS locations + discovery (`GET /v1/organizations?lat&lng&radius_km`), specialties, KYB docs + submit rules, encrypted IBAN (last4 only), plans/subscription (sets commission bps), admin KYB queue approve/reject/suspend/reactivate with reason (`/v1/admin/organizations`); Prisma P2002/P2025/P2003 mapped to CONFLICT/NOT_FOUND/VALIDATION; 11 e2e (Step 5); vehicles module — VIN normalise/decode (`VinDecoderPort` + WMI mock), Saudi plate normalisation (AR/EN, Arabic-Indic digits, tatweel), add by VIN/plate (owner user or fleet org), ownership checks, `VehicleEventsWriter` (exported; other modules record Car Passport events inside their tx), odometer monotonic, passport + share token → public masked passport (`GET /v1/passport/:token`); 4 unit + 6 e2e (Step 6); work-orders module — legal transition table + `WoTransitionService` (history + audit + outbox + realtime in one tx), items with per-line VAT totals, versioned snapshots (canonical JSON + SHA-256, DB append-only), request-approval / change orders (new version → re-approval), customer approval via Nafath sign (`NafathPort.initiateSign`) or OTP fallback with signature row storing the snapshot hash, guards (no execution before approval, check-out before delivery, customer closes after delivery, customer cancels only before approval), inspections + media links + Car Passport events, Arabic RTL document renderer (`PdfRendererPort`; HTML adapter now, Puppeteer PDF in backlog), Socket.IO `/realtime` channel `work-order:{id}` with per-channel access; 3 unit + 11 e2e (Step 7); `packages/zatca-ubl` (Phase 1 QR TLV encode/decode verified against ZATCA's published sample, canonical invoice hash, UBL 2.1 XML placeholder builder; 6 tests) + invoicing module — invoice issued from the last signed WO version (lines from the snapshot; totals must equal it), per-org gapless numbering via row-locked `invoice_sequences` upsert (`INV-`/`CN-`), simplified vs standard by buyer, buyer/seller snapshots, void (unpaid only), credit notes (full/partial, full CN voids unpaid parent), RTL HTML document + XML endpoints (`InvoiceRendererPort`); 2 unit + 7 e2e (Step 8); payments module — `PspPort` + HMAC-signed mock (intent → webhook; `POST /v1/payments/:id/mock-pay` dev hook), webhook inbox idempotency (`webhook_events` unique per vendor event), `EscrowService` (hold/release/freeze/refund, every move = balanced ledger entry via `@sinaaty/ledger`, DB trigger re-checks), release rules (customer `confirm-receipt`, auto-release job after `ESCROW_AUTO_RELEASE_HOURS`, frozen holds skipped, admin release/freeze/refund with reason), commission split by org bps + VAT on commission, payouts (bundle released holds per org → execute → `org_available → bank`), org wallet (`/v1/organizations/:id/wallet`), cash payment with customer OTP (no escrow), `/v1/admin/ledger/health`; in-process schedulers (`JOBS_ENABLED`); 9 e2e incl. DB-level 'ledger always balanced' (Step 9); integrations module — in-process **outbox dispatcher** (`OutboxProcessor` polls `outbox`, per-(event×handler) retries with backoff + dead-letter tracked in `integration_requests`, `OutboxHandlerRegistry`; `/v1/admin/outbox/drain|retry`); promissory-notes module — `NafezPort` + mock, note issued on deferred approval (draft → Nafez → issued, idempotent), **close-on-payment in one handler** (Nafez close → note closed → settlement `MK-` with hash → passport), partial settlement, cancel on WO cancel, dunning job (`DUNNING_SCHEDULE_DAYS`, formal step) idempotent per note+step, enforcement request after formal notice with evidence bundle (manifest + zip, dependency-free writer), RTL note/settlement documents; e2e serial (`--runInBand`); 3 unit + 7 e2e (Step 10); notifications module — 28 AR/EN templates in code (`domain/templates.ts`, mirrored into `notification_templates` at boot), `NotificationService` (locale render, in-app copy always, push to device tokens, SMS for legal/formal, dedupe per user+template+key), Push/SMS/WhatsApp ports + mocks, outbox handlers for WO created/approval/approved/status/change-order, invoice issued/paid, escrow released, note issued/partial/closed(settlement), dunning reminder/formal, enforcement, org status; inbox `/v1/me/notifications` (unread count, read, read-all), `/v1/admin/notifications/templates`; 4 unit + 5 e2e (Step 11). `apps/admin-web`, `packages/ui-tokens` still empty. **Phase 2 backend (Steps 6–11) complete.** Flutter foundation (Step 12): `apps/mobile` — Clean Architecture skeleton with `import_lint` rules, `Result<T>/Failure`, flavors (entrypoints + Android productFlavors; iOS schemes pending), `AppConfig` via dart-define, theme from design tokens (Plex Arabic embedded, light/dark), ARB l10n (ar default/en), go_router with auth guard, dio `ApiClient` (bearer + single-flight refresh + envelope → Failure), secure `TokenStore`, Riverpod 3 providers, shared UI kit (`core/ui`), OTP login + OTP screen + flavor tab shell, `tool/gen_api.dart` (endpoints from OpenAPI); goldens light/dark RTL with real Arabic font; 5 tests. Step 13 — customer MVP: API `ApprovalLinkService` (HMAC stateless token, `/v1/approve/:token` RTL HTML page + `/data`, `/otp`, `/complete`, `/decline`; SMS template carries the link; 4 e2e) and Flutter features `vehicles` (list/add by VIN or plate/passport+share), `work_orders` (list card, detail with live timeline via Socket.IO `WorkOrderRealtime` port, items, photos strip, ONE primary action approve→pay→confirm-receipt; approve screen renders the signed snapshot, Nafath poll or OTP, decline), `billing` (wallet: due invoices → notes → history; invoice + pay sheet mada/Apple Pay via mock PSP; note + settlement), `notifications` (inbox, deep-link `sinaaty://` → route, unread badge), `account` (profile, language, logout); shared `Fmt` (Western digits), `Labels`, `StatusTimeline`, `AppListRow`, `KeyValueRow`, `InlineError`, `AsyncResultView`; primary action lives in a fixed bottom bar; 8 tests incl. approve→pay flow + 5 goldens. **UI v2 identity** (`docs/design/sinaaty-ui-v2.html`, product map `docs/design/sinaaty-product-map.html`): `SealCard`/`SealButton`/`SealPill`/`SealSteps` signature surface, `FloatingNav`, `SectionTitle`, `MoneyText`, `BrandMark`. Step 14 — workshop app: `features/workshop` (`WorkshopRepository` + `PendingActions` port with durable JSON-file impl, `SyncController` queues status/media actions on `NetworkFailure` and replays in order), screens Today (KPIs → needs-action SealCard → today's cars, `+ أمر جديد` in header), Orders (filters), New order (phone/plate|VIN/title/items sheet/terms, live estimate), Workshop order (one primary action per state: receive → inspect → request approval → start → QC → ready → invoice → deliver; add item/photo in more), Check-in inspection (8 angles camera→presign→upload, body notes, odometer/fuel; `pickImage` injectable), Org wallet; 3 tests incl. full happy path + offline→online sync + 4 goldens. **Parts module** (Steps 18/18b, `apps/api/src/modules/parts`): `PartsRepository` (Prisma) over catalog/fitments/inventory/requests/bids/orders/trade accounts/serials/warranties/group buys; `CatalogUseCases` (brand/catalog/fitment upsert, CSV import, inventory upsert by `external_sku` + CSV sync runs, `GET /v1/parts/fit?vin=` → VIN decode → fitments → live offers sorted by price with trade prices when an active trade account exists); `MarketplaceUseCases` (request → PostGIS `matchSuppliers` → recipients + outbox, one live bid per supplier via upsert, accept → order, expiry job, realtime `part-request:{id}`); `OrdersUseCases` (from bid / Buy Now, prepaid → supplier invoice via `InvoicesUseCases.issueForPartOrder` → PSP → escrow with `partOrderId`; deferred → `creditAllows` check on trade account → `NotesService.issueForPartOrder` via outbox; transitions with stock reserve/decrement; confirm → escrow release + part warranties; auto-confirm job; `issueWarranty` with content hash + passport event); `SerialsUseCases` (agent batches with QR tokens, transfer on sale, public `/v1/parts/verify/:token` with scan counting + alert, install → `part_and_labor` warranty + passport, duplicate-install alert); `TradeAccountsUseCases` (request/approve/hold, outstanding cached ± on order/note close); `WarrantiesUseCases` (mine/issued/public check/claims/resolve + minimal group buys); handlers: InvoicePaid → order paid, PartOrderCreated(deferred) → note, SettlementIssued → outstanding; seed adds a parts dealer (+966500000004) and VAT numbers; 2 e2e suites (14 tests). **Mobile parts (Step 22)**: `features/parts` (domain entities + `PartsRepository` + `QrScanner` port, dio impl + `mobile_scanner` impl), workshop «القطع» tab (trade-account SealCard, VIN search → offers with trade price/QR/warranty badges → Buy Now sheet (qty, prepaid/deferred) → order; reverse-auction sheet; my auctions/orders; QR install flow: scan → verify → pick WO part item → install → warranty), `PartRequestScreen` (requester: bids + accept sheet; supplier: bid/update sheet), `PartOrderScreen` (role-aware primary: pay/confirm vs prepare/ship/deliver), supplier tabs `طلبات · مبيعاتي · محفظتي` chosen by org type via `currentOrgInfoProvider` (`SupplierRequestsScreen` hot-request SealCard, `SupplierSalesScreen` orders to fulfil + trade accounts approve sheet + inventory + QR batch issue); 4 tests + 3 goldens (15 mobile tests total). **Admin back-office (Step 15)**: API `modules/admin` (`AdminQueryRepository` read-model: `/v1/admin/overview` KPIs incl. integration health + ledger imbalance, `/audit` + `/audit.csv` export, `/settings` + `PUT /settings/:key` (super-admin + reason), `/integrations` (requests/webhooks/dead-letters) + `/integrations/retry`, `/payments|/escrow|/payouts`, `/users` + `PUT /users/:id/platform-role`; 5 e2e). `apps/admin-web` — Next.js 15 App Router + Tailwind (tokens from UI v2: seal card, pills, cards) + TanStack Query, RTL Arabic, OTP login gated on a platform role, pages: نظرة عامة · المنشآت وKYB (+ملف المنشأة: اعتماد/رفض/إيقاف بسبب) · التكاملات (DLQ/طلبات/Webhooks/سندات معلّقة) · الدفعات والضمان (تحرير/تجميد/استرداد + التحويلات + صحة الدفتر) · السندات · النزاعات (placeholder) · الإعدادات · المستخدمون · سجل التدقيق (+CSV); `ReasonDialog` enforces a written reason on every sensitive action; Playwright smoke green. **Disputes (Step 16)**: `modules/disputes` — open on a work order/part order (party or staff) → escrow hold frozen + WO moved to `disputed` via `WoTransitionService` in the same tx; evidence via `media_links`, messages with ops-only `is_internal` notes; ops assign/status (`DISPUTE_TRANSITIONS`, closing blocked while money is frozen) and `resolve` (`release_to_provider|refund_customer|split|replace_part|no_action`) posting refund/release through `EscrowService` in one tx (`decisionAmounts` guards the split range), PSP settled after commit via `EscrowUseCases.settleRefundWithPsp`; reviews after delivery (one per reviewer per order) refresh `organizations.rating_avg/count`; 3 notification templates (opened/message/resolved) with `sinaaty://disputes/{id}`; admin disputes room in admin-web (conversation + internal notes + decision panel with live split preview); 6 e2e (incl. frozen escrow skipped by auto-release and ledger imbalance 0.00 after a split). **CI/CD + observability (Step 17)**: `.github/workflows/ci.yml` (packages · api with a real PostGIS service and per-run Ed25519 keys · admin-web with Playwright smoke · mobile analyze/import_lint/test · GHCR images on main) + `codeql.yml`; `apps/api/Dockerfile` and `apps/admin-web/Dockerfile` (multi-stage, non-root, healthchecked; migrations run as a Helm pre-upgrade Job, never on app start); `infra/helm/sinaaty` (api/admin/migrate-job/ingress, secrets from the cluster, integrations still mock); OpenTelemetry in `src/telemetry.ts` started **before** the app is imported (`main.ts` → dynamic `bootstrap.ts`) with HTTP/Nest/**Prisma** spans and health probes excluded, `infra/observability/otel-collector.yaml` (PII scrubbing) + Grafana dashboard incl. a ledger-imbalance panel. Fixed on the way: `openapi:export` was broken under tsx (esbuild drops `emitDecoratorMetadata` → Nest DI fails) and now runs against `dist/`; `prisma:drift` no longer needs a shell `DATABASE_URL`; `pretest:e2e` warns when a dev API is racing the suite. **Logistics (Step 19)**: `modules/logistics` — `MapsPort` + mock (haversine × road factor, ETA), domain `quotePrice` (base + per-km × type multiplier, minimum fee, margin bps) and `TRANSPORT_TRANSITIONS` (no cancelling once the car is loaded; delivery needs `canComplete` = photo **and** OTP), PostGIS repository (geography columns are projected as lat/lng — never `SELECT *`, Prisma cannot deserialize them), use cases: quote · request (freezes price + margin) · driver profile/online · nearby offers · first-to-accept wins (re-checked inside the tx) · driver-only state machine · rate-limited tracking (`TRANSPORT_TRACK_MIN_SECONDS`) with realtime `transport:{id}` · receiver OTP (`accept_delivery`) + photo proof → delivered + Car Passport event; 4 notification templates; 6 unit + 8 e2e. **ZATCA Phase 2 (Step 20)**: `packages/zatca-ubl` grew a real compliance core — hand-written DER encoder + X.509 reader/issuer (`der.ts`, `x509.ts`), **XML C14N 1.1 subset** (`c14n.ts`, 8 rule tests) because the invoice hash is the SHA-256 of canonical bytes, full UBL 2.1 (388/381/383, standard vs simplified, ICV/PIH/QR references), `hash.ts` (strips UBLExtensions + QR + Signature), **XAdES-B-B signing** with secp256k1 and raw `r‖s` signatures plus independent `verifySignedInvoice`, Phase-2 QR tags 1–9, and a **PKCS#10 CSR** carrying ZATCA's template OID and EGS SAN attributes (OpenSSL verifies it). API: `ZatcaPort` + mock (issues a real CSID certificate and **verifies every submitted signature**) + HTTP sandbox adapter, `zatca_devices`/`zatca_submissions` repository with a **row-locked ICV/PIH chain**, `ZatcaService` (onboard = CSR → compliance CSID → compliance check → production CSID, CSID stored AES-256-GCM encrypted; sign → clear (B2B) / report (B2C) → persist hash/QR/signed XML), automatic submission on `InvoiceIssued` through the outbox (retries + DLQ), endpoints under `/organizations/:id/zatca/*` and `/invoices/:id/zatca*`; migration adds `invoices.zatca_xml` (6-year archive); 23 package tests + 6 e2e. **Security & concurrency hardening (2026-08-20):** boot refuses production with committed dev secrets (`APPROVAL_LINK_SECRET`/`PSP_WEBHOOK_SECRET`/`NAFATH_CALLBACK_SECRET`) or an all-zero `PII_ENC_KEY`; the OTP code is returned in the response only when `!isProd && INTEGRATION_SMS=mock` (the public approval page reaches that path, so returning it defeated the second factor for anyone holding the SMS link); the per-phone OTP quota is enforced on the public approval and proof-of-delivery paths too (+ per-IP throttles) so a leaked link is not an SMS pump; `OutboxProcessor` claims rows with `FOR UPDATE SKIP LOCKED` + a 120s `locked_until` lease (multi-replica safe, released on success and failure); schedulers run under `LeaderLock` (`job_locks` lease table — advisory locks leak under Prisma's pool, proven by test) — see `docs/adr/0009-postgres-outbox-not-kafka.md`. Accidents module (Step 21) — `AccidentReportsPort` + deterministic منجز/تقدير mock (forced states in the reference itself: `…PEND…` unpriced, `…REJ…` rejected, non-`ACC` not found), `accident_reports` table, lookup (nothing stored) → link (work order becomes `source=accident_claim`, `accident_report_ref` set, Car Passport event, customer told what the insurer covers and what they pay), refresh, and `submit-repair` (FR-WO-10, idempotent, refused before the file is priced or the car is finished); the assessor's damages become draft work-order items (`suggestItems`) and `customerShare` computes deductible + fault share capped at the total; no live adapter — the module refuses to boot with `INTEGRATION_ACCIDENTS=live` because the provider contract is unconfirmed (PRD risk R3, `docs/integrations/monjez.md`); 10 unit + 9 e2e. Step 23 — customer «اطلب» tab: `RequestHubScreen` (two choices only — قطعة غيار / سطحة — plus a live-tow SealCard and the customer's open requests), part request sheet (name + car → VIN attached automatically → conditions → window) landing on the bids screen, which now ranks offers cheapest-first (`sortedForCompare`) and says *why* each one wins (`bidHighlights` → الأرخص/الأسرع/أطول ضمان); `features/transport` (`TransportRepository` over `/v1/transport/*`, `TowRequestScreen` shows the price **before** the request is sent — pickup/dropoff come from a pasted maps link or coordinates via the pure `parseLocationLink` (no location plugin yet; backlog), `TowJobScreen` with the 4-step timeline, driver card and cancel-before-loading), `WarrantiesScreen` (ساري vs منتهٍ) reachable from محفظتي; 7 new tests incl. 4 goldens (22 mobile tests). Step 24 — security review (`docs/security/review-2026-08.md`, 10 findings fixed) pinned by `test/security.e2e-spec.ts` (14 tests: default-deny, forged/tampered JWT, admin-role refusal, IDOR across work orders/invoices/vehicles/orgs, masked public passport, forged PSP webhook, OTP lockout, log/URL token masking, PII-free audit); fixes: `maskUrl` + wider pino redaction (phone, code, signature headers), `algorithms:['EdDSA']` on verify, timing-safe Nafath dev secret, mock-upload 404 outside mock storage, 20/min throttle on the public token lookups. k6 in `infra/load/k6/` — work-order intake 20 VUs → 79 req/s, create p95 52ms, transition p95 9.9ms, 0% failures; bidding 20 VUs → 49 req/s, open p95 22ms, 0% (the first run failed 99.9% because the 120/min per-IP throttle was doing its job — load runs raise it, production does not). `apps/api/tools/backup-restore-drill.sh` dumps → restores → verifies row counts, ledger balance to the halala, and that append-only tables still refuse UPDATE (passed: 9.1MB, 19324 audit rows, imbalance 0.00). Runbooks in `docs/runbooks/`: backup-restore (PITR steps marked untested), integration-outage, money-incident, key-rotation, load-and-performance. Step 25 — pilot module (`apps/api/src/modules/pilot`): industrial zones as *configuration* (`platform_settings.pilot.industrial_zones`, codes not free text) with pure `zoneFor`/`distanceKm`; every new `organization_locations` row is auto-tagged from its point (`OrganizationsUseCases.addLocation` → optional `PilotService`), `POST /v1/admin/pilot/zones/backfill` repairs NULLs *and* legacy free-text zones; feature flags in `platform_settings.feature.*` (`{enabled|orgs|org_types|zones|pct}`, missing = off, stable per-org bucketing) resolved by `GET /v1/config` for the apps **and enforced server-side** (`assertEnabled` gates group buys via `subjectFor(org)`); `analytics_events` table (unique per event×entity) fed by `AnalyticsOutboxHandlers` from 13 domain events — org/entity/amounts only, zone denormalised, replay-safe; `/v1/admin/pilot/funnel|by-zone|activation` for ops and `/v1/pilot/funnel?org_id=` for a workshop's own numbers; `tools/onboard-pilot.mjs` (owner → org → location+zone → specialties → subscription → membership, idempotent by CR, writes a **hash-chained** audit row with the ops reason; verified end-to-end on the dev DB); runbook `docs/runbooks/pilot-operations.md`. 9 unit + 7 e2e. Step 26 — Fleet Hub (`apps/api/src/modules/fleet`): pure `decide(policy, {total, workshopOrgId, monthToDateSpend})` → `auto | one_approver | two_approvers | workshop_not_allowed | over_budget` (blocking checks run first — no point asking two people to approve the impossible); `fleet_policies` CRUD, `fleet_approvals` recorded **per version** so a change order re-opens the decision, and `FleetUseCases.assertMaySign` is called from `approveInit` **and** `approveComplete` — the policy is an internal control layered on the legal signature, never a replacement; `POST /fleet/:org/vehicles/import` delegates row-by-row to `VehiclesUseCases.add` (one bad row never loses the others); monthly `fleet_statements` (idempotent per period) + `overview` (vehicles, open repairs, awaiting decision, budget used/remaining) + per-vehicle spend, all exportable as UTF-8-BOM CSV with Arabic headers. 9 unit + 8 e2e. **Local e2e hygiene:** the suite's fixed fixtures collide on a long-lived dev database — `pnpm --filter api test:e2e:clean` (`tools/e2e-clean-db.sh`) builds a scratch DB from the migrations, seeds it and runs there; 157/157 green three times in a row that way. Step 27 — voice to invoice (`apps/api/src/modules/voice`): pure Arabic extraction (`extractItems`, `parseSpokenNumber`, `priceFrom`) reading Arabic-Indic digits and spoken amounts («مئتين وستين» → 260, «ألف وخمسمئة» → 1500), quantity («عدد 2»), and item type from the leading verb; `SpeechToTextPort` (mock only — no vendor chosen, `docs/integrations/speech.md`) and `ItemExtractionPort` with a **Claude adapter whose prices are verified against the words actually said** and which falls back to the offline rules on any failure; `/v1/work-orders/:id/voice-notes` → transcript + proposals (nothing touches the order), review payload shows `heard_ar` and `needs_price` per line, `/voice-notes/:id/apply` adds **the reviewer's** lines through `WorkOrdersUseCases.addItem` (so versioning, VAT and re-approval behave identically to typing), `/discard`; 9 unit + 6 e2e. Step 28 — AI inspection (`apps/api/src/modules/vision`): pure `mergeDamages` (the inspector always wins on a zone they judged; a model finding on an untouched zone is added **marked** `source:'ai'` with its confidence, below 0.6 dropped) and `diffDamages`/`diffSummaryAr` comparing check-in with check-out → appeared / worsened / repaired / unchanged, in Arabic, with the photos — the answer to «هل تضررت سيارتي عندكم؟» before the car leaves; `VisionPort` + deterministic mock (same photo id → same finding, so identical photo sets diff to nothing), `/inspections/:id/analyze` (gated by the `ai_inspection` flag), `/confirm-damages` (accepted suggestions become the inspector's own record), `/work-orders/:id/inspection-diff` (readable by the customer too; says «بانتظار فحص التسليم» rather than implying all-clear). **No live adapter**: sending a customer's car photos to a third party is a data-residency decision first (`docs/integrations/vision.md`), so the module refuses `INTEGRATION_AI=live`. 13 unit + 7 e2e. Step 29 — abandoned vehicles (`work-orders/domain/abandoned.ts` + `application/abandoned.use-cases.ts`): pure `noticeSchedule` (three notices, the last **formal**), `nextDueNotice` (idempotent per step), `storageFee` (free until `ABANDONED_STORAGE_FREE_DAYS`, then per-day), `canDeclareAbandoned` (refuses unless the car was ready for `ABANDONED_NOTICE_DAYS` **and** every notice was actually sent) and `abandonedClaim`; `GET/POST /v1/work-orders/:id/abandoned[/notice|/declare]`, hourly `LeaderLock` job that sends due notices but **never declares** (a machine may remind; only a person declares), claim frozen at declaration into `metadata.abandoned_claim`, transition through `WoTransitionService` so history/audit/outbox behave normally, 2 notification templates (push + SMS); `requestEnforcement` now marks `is_abandoned_vehicle` and claims the frozen `storage_fees_claimed`. Notices live on `work_orders.metadata` (three rows per car, read only here) behind repository methods — `application/` never touches Prisma. 13 unit + 8 e2e. Step 30 — discovery search (`apps/api/src/modules/search`): `SearchPort` with an Arabic-normalising in-memory mock (ة/ه، أ/إ/آ/ا، ى/ي، tashkeel; token prefix + one-edit typo) and a **Meilisearch adapter over plain HTTP** (index settings pushed idempotently, filterable type/city/zone, rating in the ranking); `SearchSyncService` reindexes every active org at boot (the index is a cache, the DB is the truth) and follows `OrganizationStatusChanged` through the outbox so suspension removes from search in the same breath; discovery (`GET /v1/organizations?q=`) goes search-first for typed text with **SQL+PostGIS fallback on any failure** — finding a workshop slightly worse beats finding nothing. Live-proven against real Meilisearch (brew, :7700): misspelt «ورشه النور» found, filters honoured, removal effective. Multi-city is configuration (zones include Jeddah; discovery filters by city); k6 re-run shows no WO-path regression (create p95 55.6ms, 0% failures). 9 unit + 4 e2e. Local DB without Docker: `apps/api/tools/dev-db.sh start` (PostgreSQL 17 + PostGIS via Homebrew — Docker Hub is unreachable on the dev machine). **Owner walk on a real device (2026-08-23, `docs/design/owner-walk-2026-08-23.md`)** — six defects that 209 e2e + 72 mobile tests all passed over: the signature badge claimed «موقّع بنفاذ» over an OTP signature (now `signedMethod` travels with the version and the badge names the real method), the app took `orgs.first` blindly (now `GET /organizations/mine`, active first), an OTP user could never set a name (now `PATCH /v1/me`, refused over a Nafath-verified name), the order detail never said which workshop (now `orgNameAr`), an open tab never refreshed (now a quiet 45s invalidate of what is on screen), and `demo-day.mjs` re-planted every scene each run (now each scene is looked up live; `demo-tidy.mjs` ends duplicates through the real endpoints). **Rule: no UI step is ✅ until it is opened on a device against the real API — a golden proves the pixels did not change, not that the screen tells the truth.**

**Post-Step-30 hardening round (2026-08-21/22, three coordinated Claude sessions — api/build · mobile polish · review/requirements):** P1 pre-pilot gaps closed per `docs/design/p1-scope-2026-08.md` — the app consumes `GET /v1/config` (disabled features leave the screen), dispute screens in `features/disputes` behind the `disputes` flag with party access to evidence photos via `media_links` of type `dispute`, and **tow payment end to end**: invoice auto-issues on proven delivery (`issueForTransportJob`, idempotent per job, seller must be VAT-registered, `invoices.transport_job_id`), rides the ordinary payment line, releases in the SAME transaction when paid after proof (the receiver's OTP was the confirmation) with the split taken from the job's frozen margin into `platform_revenue:transport_margin` (+VAT), and `/v1/transport/quote` returns `{price, vat, total}` computed by the invoice line's own arithmetic (consumer quotes are VAT-inclusive). Compliance fixes: credit/debit notes carry the ORIGINAL invoice number in BillingReference (both Phase-2 signing and Phase-1 XML); search hydration is by ids (the 2×limit window silently dropped hits once active orgs passed ~40). **The intermittent whole-suite e2e failure was root-caused**: supertest's per-request wildcard listeners are hijackable on macOS by any process binding 127.0.0.1 on the same port — every suite now binds once to `127.0.0.1` in beforeAll; Linux/CI was never exposed; 12 consecutive clean runs. **OTEL metrics** (`MetricsService` in CommonModule) emit the exact names the Grafana panels query (`sinaaty_outbox_pending`, `sinaaty_ledger_imbalance`, `sinaaty_integration_requests_total{provider,status}` + `_stalled`/`_dead_letters` for alert rules), one batched SQL per export, no-op with OTEL off; they caught a real defect in minutes — the outbox dispatcher was capped at 300 events/min (batch 50/10s) and now **drains until empty each tick** (ate a 15k backlog in <30s). Two soak attempts turned into accidental chaos tests the system passed (8,000 req/s for 45 min, then a 900 iter/s OTP-invalidation storm — imbalance 0.00 throughout, instant recovery; recorded with numbers in `docs/runbooks/load-and-performance.md`; k6 scripts fixed: login in `setup()` only + every failure path pauses; runs longer than the 15-min token TTL start the API with `JWT_ACCESS_TTL_SECONDS>=run` — the clean 1-hour soak still awaits a machine awake for one continuous hour). **Maker/checker on escrow refunds** (`admin_approvals`, design + arbitration in `docs/design/maker-checker-refunds.md`): refund records a request (202), a DIFFERENT staff member approves with payload revalidation at execution time, the two-people rule is a DB CHECK, the maker may withdraw, auto-release skips holds under an open request, expiry from `platform_settings`, finance desk notified on requests and on money-moving dispute decisions; admin-web approvals inbox live-verified. Media pipeline closed end to end (hand-written SigV4 proven against AWS's doc vector + real MinIO; download URLs signed only after ownership proof; `MediaThumb` in both apps; mock-download sends `cross-origin` CORP like real S3). Coordination protocol among sessions: explicit-path `git add` only, message before touching another session's lane. Clean-DB suite 193/193; api units 124; ledger 12; mobile 39+.

**Schema workflow (Step 2 decision):** `docs/db/schema.sql` → `pnpm --filter api schema:sync` (copies it verbatim into `prisma/migrations/00000000000000_init/migration.sql`) → `prisma migrate deploy` → `prisma db pull` → `pnpm --filter api prisma:normalize` (PascalCase models / camelCase fields + `@map`) → `prisma generate`. `pnpm --filter api db:verify` = sync-check + migrate + seed + drift-check (CI). Any later schema change = a new SQL migration file **and** the same edit in `docs/db/schema.sql` **and** re-running `schema:sync` — which means every delta migration must survive re-application onto a database that already carries it (`IF NOT EXISTS` / `DO $$ … EXCEPTION WHEN duplicate_object`): the init migration is periodically rewritten to the full current schema, so deltas re-apply over their own effects (proven the hard way 2026-08-29 — three sessions skipped the sync, and a bare `CREATE TRIGGER` killed clean-DB builds).

## 3. Tech stack (summary — details in docs/03-TECH-STACK.md)

- **Mobile/Web apps:** Flutter 3.x / Dart ≥3.13, Riverpod (codegen), go_router, dio+retrofit, freezed, drift (offline), socket_io_client, ARB i18n (Arabic default, RTL first).
- **API:** NestJS 11 on Node 22, TypeScript strict, Prisma 6 + raw `pg` for ledger, Zod validation (`nestjs-zod`), BullMQ on Redis 7, Socket.IO gateway, pino logs, OpenTelemetry.
- **DB:** PostgreSQL 16 + PostGIS + pg_trgm + pgcrypto. Redis 7. S3-compatible object storage. Meilisearch.
- **Admin:** Next.js 15 App Router, shadcn/ui, TanStack Table/Query.
- **Tooling:** pnpm workspaces + Turborepo, ESLint/Prettier, Husky + commitlint (Conventional Commits), Vitest/Jest + Supertest + Testcontainers, flutter_test + integration_test.

## 4. Commands

```bash
# ---- Backend (apps/api) ----
pnpm install
pnpm --filter api dev                 # nest start --watch
pnpm --filter api test                # unit
pnpm --filter api test:e2e            # supertest + testcontainers (needs Docker)
pnpm --filter api lint
pnpm --filter api prisma:migrate      # prisma migrate dev
pnpm --filter api db:verify           # schema sync-check + migrate deploy + seed + drift-check (CI)
pnpm --filter api prisma:generate
pnpm --filter api seed                # dev seed (makes, categories, plans, demo orgs)
node apps/api/tools/demo-day.mjs      # plant the touchable demo day (docs/pilot/demo-day.md)
pnpm --filter api openapi:export      # writes packages/shared-types/openapi.json

# ---- Infra ----
docker compose -f infra/docker/docker-compose.yml up -d   # postgres(postgis), redis, minio, meilisearch, mailpit

# ---- Mobile (apps/mobile) ----
flutter pub get
dart run build_runner build --delete-conflicting-outputs
flutter run --flavor customer --dart-define-from-file=env/dev.json -t lib/main_customer.dart
flutter run --flavor partner  --dart-define-from-file=env/dev.json -t lib/main_partner.dart
flutter test
flutter analyze
dart run tool/gen_api.dart            # OpenAPI → lib/core/api/generated

# ---- Admin (apps/admin-web) ----
pnpm --filter admin-web dev

# ---- Whole repo ----
pnpm turbo lint test build
```

Until step 0 is done, the only working commands are the plain Flutter ones at repo root (`flutter pub get`, `flutter analyze`, `flutter test`).

## 5. Engineering rules (non-negotiable)

### 5.0 Simplicity charter (overrides everything below when in conflict)

The owner's directive: **no distraction, no complexity — simple flows, calm high-quality design, one integrated product.** The platform is large underneath; the surface must feel small.

1. **One primary action per screen.** Every screen answers one question and has one big obvious button. Secondary actions go behind a "more" menu.
2. **Three to four tabs, never more.** Customer app: `سياراتي · اطلب · محفظتي · حسابي`. Partner app: `اليوم · الأوامر · القطع · المحفظة` (role decides what's inside). Scrapyard sees only `طلبات · مبيعاتي · محفظتي`.
3. **Progressive disclosure.** Advanced features (trade accounts, group buys, analytics, fleet policies, voice, AI) are hidden until the user actually needs them or their role/plan enables them. Default screens show only MVP essentials.
4. **Two-tap rule for daily tasks:** create work order, update status, approve, pay, submit bid, scan QR — each reachable in ≤ 2 taps from the home tab.
5. **Copy is short and human.** Arabic first, plain words, no jargon on screen (never "escrow"/"outbox"/"snapshot" — say "المبلغ محفوظ حتى تستلم"). One sentence of explanation max under any legal action.
6. **Calm visuals.** One accent color (seal green) + one secondary (brass) used sparingly; semantic colors only for state; generous spacing; no decorative animation; no dense dashboards on mobile (charts live in web only).
7. **Empty states teach**, errors say what to do next, loading never blocks the whole screen.
8. **Consistency over cleverness.** Same components everywhere (`packages/ui-tokens` + a shared Flutter component library `lib/core/ui/`): cards, badges, buttons, timeline, list rows. No one-off widgets.
9. **Cut before you add.** When a step asks for a feature, ship the minimal version that completes the flow end-to-end; log extras in `docs/backlog.md` instead of building them.
10. **Design review gate:** each Flutter step must include screenshots (light/dark, RTL) and pass the checklist above before it is marked ✅.

### 5.1 Money, time, IDs, text
- Money: DB `NUMERIC(14,2)`; TS `Decimal` (`decimal.js`); Dart `Decimal` (package `decimal`). **Never** `number`/`double` for amounts. Rates in basis points (`_bps`) or `NUMERIC(5,2)` percentages.
- VAT default 15%; compute per line, round half-up to 2 dp per line, sum lines (ZATCA rule).
- Time: store `timestamptz` UTC; display `Asia/Riyadh`; Hijri only at presentation layer.
- IDs: UUIDv7 generated in app (`uuidv7` pkg / Dart `uuid` v7). Human numbers via SQL `next_number('WO')`.
- Text: `_ar` mandatory, `_en` optional. UI strings in ARB files, never hard-coded Arabic in widgets.

### 5.2 Domain integrity
- State transitions go through the module service's `transition()` only (work orders, promissory notes, escrow, part orders, transport). Every transition writes a history row + audit log + outbox event in the **same DB transaction**.
- Approval snapshots (`work_order_versions`) are immutable; the customer signs the `snapshot_sha256`. Changing scope after approval = new version + new signature.
- Ledger: every financial effect is a balanced `ledger_entries` + `ledger_lines` posting via `packages/ledger`. Never mutate balances directly. Never post outside a transaction.
- Append-only tables (audit_log, ledger_entries, work_order_versions, work_order_signatures, settlements, promissory_note_events): no UPDATE/DELETE, ever — the DB trigger will reject it; do not remove the trigger.
- Idempotency: every outbound integration call and every inbound webhook carries an idempotency key persisted in `integration_requests` / `webhook_events`.

### 5.3 Integrations (Anti-Corruption Layer)
- Each provider lives in `apps/api/src/modules/integrations/<provider>/` with: `<provider>.port.ts` (interface), `<provider>.adapter.ts` (real), `<provider>.mock.adapter.ts` (dev/test), `<provider>.mapper.ts`.
- Domain modules depend on the **port**, never on the adapter or provider SDK.
- Provider selection by env: `INTEGRATION_NAFATH=mock|live` etc. Default in dev/test = `mock`. CI must pass with all mocks.
- Never log PII or secrets from provider payloads; use the redaction helper before persisting `request_payload`.
- Nafath / Nafez / Najiz / Monjez APIs are contractual and may differ from assumptions — implement against the port, keep the adapter thin, and document the assumed contract in `docs/integrations/<provider>.md`.

### 5.4 Security & privacy
- Nafath (OIDC) for individuals; OTP for operational roles. Access JWT 15 min, refresh rotation with family reuse detection.
- Encrypt `national_id`, `iban`, CSIDs with AES-256-GCM (`packages/../crypto` helper); keys from env/KMS, never in repo.
- RBAC guards + resource ownership checks in every controller. Default deny.
- No PII in logs, URLs, or analytics. Redact in pino serializers.
- Data residency: no external SaaS that stores PII outside KSA without an ADR.

### 5.5 Clean Architecture (mandatory in both apps)

**Dependency rule:** source code dependencies point inward only: `presentation/interface → application → domain`; `infrastructure` implements domain/application ports and is wired only at the composition root (Nest module / Riverpod provider overrides). Domain never imports Nest, Prisma, Dio, Flutter, or any provider SDK.

**Backend — every NestJS module (`apps/api/src/modules/<name>/`) is a bounded context with 4 layers:**
```
<name>/
├── domain/            # pure TS: entities, value objects (Money, Vin), enums, domain events,
│                      #   state machines (transition tables), domain errors, repository PORTS (interfaces)
├── application/       # use cases (one class per command/query, e.g. approve-work-order.use-case.ts),
│                      #   DTOs (Zod schemas), application ports (NafathPort, ClockPort, IdGeneratorPort, OutboxPort)
├── infrastructure/    # Prisma repositories implementing domain ports, provider adapters (+ .mock.adapter.ts),
│                      #   mappers (Prisma row ⇄ entity), BullMQ processors, event subscribers
├── interface/         # http/ (controllers, guards, request/response mappers), ws/ (gateways), cli/
├── <name>.module.ts   # composition root: binds ports → adapters based on env (INTEGRATION_*=mock|live)
└── __tests__/         # domain + use-case tests run without DB (in-memory repos); infra tests use Testcontainers
```
Rules: controllers contain no business logic (parse → use case → map response). Use cases own the transaction boundary (`UnitOfWork` port). Cross-module calls go through the other module's **application service/port**, never its repository or tables. `packages/ledger`, `packages/zatca-ubl` are pure domain/application libraries with zero framework imports.

**Mobile — every Flutter feature (`apps/mobile/lib/features/<feature>/`) has 3 layers:**
```
<feature>/
├── domain/        # entities (freezed), value objects, repository interfaces, use cases (plain Dart)
├── data/          # DTOs (json_serializable), remote (generated API client) & local (drift) data sources,
│                  #   repository implementations, mappers DTO ⇄ entity
└── presentation/  # Riverpod controllers/notifiers (call use cases), screens, widgets (no logic)
lib/core/          # shared: api client, auth, theme, l10n, routing, error mapping, DI overrides per flavor
```
Rules: widgets never call Dio/drift directly; state lives in Riverpod notifiers; use cases return `Result<T, Failure>` (no throwing across layers); the generated OpenAPI client is used only inside `data/`.

**Enforcement:** ESLint `import/no-restricted-paths` (api) and `dart_code_metrics`/`import_lint` (mobile) forbid inward-layer violations; CI fails on violation. Add an ADR (`docs/adr/0007-clean-architecture.md`) in Step 0.

- **الحارس في طبقة واحدة ليس حارساً.** أي حقل يظهر في مستند قانوني (الرقم الضريبي، السجل التجاري) أو يعمل هوية دخول (الجوال) يُحرس بقيد في قاعدة البيانات لا في الـDTO وحده — البذور والأدوات و`UPDATE` اليدوي تكتب تحت الطبقة، فيظهر الخلل في أعمق نقطة وبأسوأ رسالة وفي أسوأ لحظة. والحقل **المعمّى** (IBAN، الهوية الوطنية، CSID) تسقط عنه حراسة القاعدة حتماً — فحارسه الوحيد دالة التشفير المركزية، لا نسخة تحقق في كل مستدعٍ (`docs/design/identity-fields-audit.md`).
- **الاختبارات تختبر ما نعرف أننا بنيناه، والفاحص يختبر ما يفعله الناس** (`tools/seam-check.mjs` في CI): ثلاثة أعطال صامتة في يوم واحد عاشت بين حزمتَي اختبارات خضراوين لأن كل طرف اختبر افتراضه ولم يختبر أحد الوصلة. وما لم يُنظر إليه على شاشة حقيقية لم يُثبت أنه يعمل.

### 5.6 Code style
- TypeScript `strict: true`, no `any` (use `unknown` + Zod). ESLint + Prettier. Files kebab-case; classes PascalCase; DB snake_case (Prisma `@map`).
- NestJS module layout follows §5.5 (`domain/ application/ infrastructure/ interface/`), plus `<name>.module.ts` and `__tests__/`.
- Flutter: feature-first per §5.5, Riverpod providers with codegen, `freezed` models, no business logic in widgets. `very_good_analysis` lints.
- Commits: Conventional Commits (`feat(work-orders): …`, `fix(payments): …`). Small PRs, one module at a time.
- Tests are mandatory for: state transitions, ledger postings, VAT math, ZATCA QR/XML, escrow release rules, note close-on-payment. Aim ≥ 80% on `payments`, `promissory-notes`, `invoicing`, `work-orders`.

### 5.7 What NOT to do
- Don't add a Go/other-language service without an ADR.
- Don't call provider SDKs from domain services.
- Don't store amounts as floats or strings.
- Don't hardcode fees/limits — read from `platform_settings` / `subscription_plans`.
- Don't ship UI that is LTR-only or has English as the default locale.
- Don't create tables outside `docs/db/schema.sql` — update the SQL and the Prisma schema together.

## 6. Domain glossary (AR ↔ EN ↔ code)

| عربي | English | Code |
|---|---|---|
| أمر عمل / أمر إصلاح | Work order | `work_orders`, `WorkOrder` |
| فحص الاستلام / التسليم | Check-in / check-out inspection | `inspections.type = check_in/check_out` |
| اعتماد عبر نفاذ | Nafath approval/signature | `work_order_signatures.method = nafath` |
| سند لأمر إلكتروني | Electronic promissory note | `promissory_notes` (Nafez) |
| مخالصة | Settlement / clearance | `settlements` |
| تنفيذ (ناجز) | Enforcement | `enforcement_cases` |
| حساب ضامن | Escrow hold | `escrow_holds` |
| فاتورة ضريبية / مبسطة | Standard / simplified tax invoice | `invoices.type` |
| طلب قطعة / مزاد عكسي | Part request / reverse auction | `part_requests`, `part_bids` |
| تشليح | Scrapyard | `org_type = scrapyard`, `part_condition = used_scrapyard` |
| وكيل / موزّع قطع | Parts distributor / brand agent | `org_type = parts_distributor | parts_brand_agent`, `parts_catalog`, `part_fitments`, `supplier_inventory` |
| حساب آجل مضمون | Nafez-secured trade account | `trade_accounts` (+ `promissory_notes` per deferred order) |
| مصادقة القطعة | Genuine part check (QR serial) | `part_serials` |
| ضمان ثلاثي (قطعة + تركيب) | Part + labor warranty | `warranties.covers = part_and_labor`, `part_serial_id`, `installer_org_id` |
| شراء جماعي | Group buy | `group_buys` |
| ضمان رقمي | Digital warranty | `warranties` |
| سطحة | Tow truck | `transport_jobs.type = flatbed_tow` |
| سجل المركبة (Car Passport) | Vehicle passport | `vehicle_events` |
| أسطول | Fleet | `org_type = fleet_company`, `fleet_*` |
| المركبة المهجورة | Abandoned vehicle | `work_orders.status = abandoned`, `enforcement_cases.is_abandoned_vehicle` |

## 7. Environment variables (apps/api/.env.example — keep in sync)

```
DATABASE_URL, REDIS_URL, S3_ENDPOINT/S3_BUCKET_MEDIA/S3_BUCKET_DOCS/S3_KEY/S3_SECRET,
MEILI_HOST/MEILI_KEY, JWT_PRIVATE_KEY/JWT_PUBLIC_KEY, PII_ENC_KEY (32B base64),
INTEGRATION_NAFATH=mock|live, NAFATH_APP_ID, NAFATH_APP_KEY, NAFATH_CALLBACK_URL,
INTEGRATION_NAFEZ=mock|live, NAFEZ_BASE_URL, NAFEZ_CLIENT_ID, NAFEZ_CLIENT_SECRET,
INTEGRATION_ZATCA=mock|sandbox|live, ZATCA_BASE_URL,
INTEGRATION_PSP=mock|live, PSP_PROVIDER, PSP_API_KEY, PSP_WEBHOOK_SECRET,
INTEGRATION_ESCROW=mock|live, ESCROW_PROVIDER, ESCROW_API_KEY,
INTEGRATION_SMS=mock|live, SMS_PROVIDER, SMS_API_KEY,
INTEGRATION_PUSH=mock|live, FCM_SERVICE_ACCOUNT_JSON (JSON أو base64),
INTEGRATION_AI=mock|live, ANTHROPIC_API_KEY, SPEECH_PROVIDER, MAPS_API_KEY, VIN_DECODER_KEY,
ESCROW_AUTO_RELEASE_HOURS=72, BIDDING_DEFAULT_MINUTES=60, ABANDONED_NOTICE_DAYS=15
```

## 8. Build plan — execute step by step, in order

Each step is a self-contained prompt for Claude Code. Do **one step per session/PR**, run its checks, then stop and report. Do not skip ahead. Mark a step done by appending `✅ <date>` to its line here.

### Phase 1 — Foundation
- **Step 0 — Restructure into monorepo.** Move the current Flutter template into `apps/mobile/` (keep git-less files intact), create `pnpm-workspace.yaml`, `turbo.json`, root `package.json`, `.editorconfig`, `.gitignore`, `infra/docker/docker-compose.yml` (postgis/postgis:16, redis:7, minio, meilisearch, mailpit), `.env.example`, `docs/adr/0001-modular-monolith.md` … `0007-clean-architecture.md`, and `git init` with an initial commit. Verify: `flutter analyze` inside `apps/mobile` passes; `docker compose up -d` starts. ✅ 2026-08-17
- **Step 1 — Scaffold `apps/api` (NestJS).** Nest CLI project, strict TS, module skeleton generator/template following §5.5 layers (`domain/ application/ infrastructure/ interface/`) + ESLint `import/no-restricted-paths` layer rules, ConfigModule with Zod-validated env, pino logger, health endpoint, Swagger at `/docs`, global exception filter with Arabic/English error envelope `{code, message_ar, message_en, details}`, request-id middleware, Throttler. Prisma init pointing to `DATABASE_URL`. Verify: `pnpm --filter api dev` serves `/health`. ✅ 2026-08-17
- **Step 2 — Prisma schema = docs/db/schema.sql.** Translate `schema.sql` into `prisma/schema.prisma` (all 79 tables, enums, `@map`s), plus a raw SQL migration for triggers/functions/views/extensions Prisma can't express (`next_number`, `assert_entry_balanced`, `audit_log_guard`, `ledger_balances`, PostGIS columns). Add materialized views `mv_org_daily_stats`, `mv_platform_daily_stats` (dashboard summaries, refreshed by a scheduled worker) in the raw migration. Add `prisma/seed.ts` (makes/models, service & part categories, plans, platform_settings, one demo workshop/scrapyard/customer). Verify: `prisma migrate dev` on fresh DB + seed; a CI script diffs Prisma vs `schema.sql`. ✅ 2026-08-17
- **Step 3 — Shared kernel.** `packages/shared-types` (enums mirrored from DB), `packages/ledger` (post(entry) with balance assertion, account resolver `escrow_liability:{org}` etc., unit tests), `apps/api/src/common/` (Decimal helpers, VAT calc, UUIDv7, crypto AES-GCM helper, PII redaction, audit-log writer with hash chain, outbox writer, `Money` value object). Verify: unit tests green. ✅ 2026-08-17
- **Step 4 — Identity module.** Nafath port + mock adapter (simulates initiate → random number → callback), OTP flow, JWT issue/refresh rotation, devices, RBAC guard + `@Roles()`, `@CurrentUser()`, ownership helper. Endpoints from Architecture §10. Verify: e2e login via mock Nafath and OTP. ✅ 2026-08-18
- **Step 5 — Organizations module.** CRUD, members/roles, locations (PostGIS), specialties, KYB documents (media upload presigned URLs → `media_assets`), bank accounts (encrypted IBAN), subscription plans/subscriptions. Admin approve/suspend endpoints. Verify: e2e create workshop → upload CR → admin approves. ✅ 2026-08-18

### Phase 2 — MVP
- **Step 6 — Vehicles module.** Add by VIN/plate, VIN decoder port + mock, ownership, `vehicle_events` writer (Car Passport), public passport token endpoint. Verify: e2e + unit. ✅ 2026-08-18
- **Step 7 — Work orders module (core).** Create/edit items, versions & snapshots with sha256, transition() with the state machine from Architecture §5.1, status history, inspections (check-in/out with media links), change orders, Nafath signing of a version (mock), auto-generate WO number, PDF rendering of the approved version (Puppeteer, RTL). Realtime gateway channel `work-order:{id}`. Verify: full happy path e2e; illegal transitions rejected; snapshot immutability test. ✅ 2026-08-18
- **Step 8 — Invoicing module (ZATCA Phase 1).** Invoice from work order, per-org sequential numbering, line VAT math, `packages/zatca-ubl` QR TLV (base64) + PDF/A-3 with embedded XML placeholder, void/credit note. Verify: QR decoding test vectors; totals match WO snapshot. ✅ 2026-08-18
- **Step 9 — Payments + Escrow + Ledger.** PSP port + mock (intent → webhook), payments, `escrow_holds` (held → auto release after `ESCROW_AUTO_RELEASE_HOURS` or on customer confirmation; frozen on dispute), ledger postings for capture/release/fee/refund, payouts scheduler + org wallet endpoints. Verify: ledger always balanced (property test), release rules e2e, idempotent webhook replay. ✅ 2026-08-18
- **Step 10 — Promissory notes (Nafez) + settlement.** Nafez port + mock, create note on approval when `payment_terms=deferred` (outbox → worker), events, close-on-payment worker (`InvoicePaid` → close note → generate settlement PDF → notify) — must complete in one job with retries; partial settlement; dunning schedule; enforcement case bundle builder (zip of note/invoice/signatures/inspection photos). Verify: e2e "approve deferred → note issued → pay → note closed + settlement PDF ≤ 60s in mock". ✅ 2026-08-18
- **Step 11 — Notifications module.** Templates (AR/EN), FCM/SMS/WhatsApp ports + mocks, in-app inbox, event subscribers for all WO/PN/payment events. Verify: each transition emits the right template. ✅ 2026-08-18
- **Step 12 — Flutter foundation (apps/mobile).** Feature/layer skeleton per §5.5 (`features/<f>/{domain,data,presentation}`, `core/`) with import-lint rules, `Result/Failure` types, flavors (customer/partner/fleet) with separate entrypoints, env via dart-define, theme + Arabic fonts + RTL, go_router with auth guard, dio client with token refresh interceptor, OpenAPI-generated client, Riverpod setup, l10n (ar/en), error handling, secure storage, Sentry. Verify: `flutter analyze`, golden test of RTL scaffold. ✅ 2026-08-18
- **Step 13 — Customer app MVP screens.** Lightweight **web approval page** (no app install: SMS link → review → Nafath approve; see docs/05-USER-FLOWS.md §2.1), Nafath login (mock deep link), vehicles list/add by VIN, work order list/detail with live tracking timeline + media gallery, approve version (Nafath flow), invoice view + pay (PSP mock sheet), promissory note & settlement view, notifications inbox, profile. Verify: integration tests for approve + pay flow against mock API. ✅ 2026-08-18
- **Step 14 — Partner app MVP screens (workshop).** Org dashboard, create work order (vehicle lookup, items, estimate), check-in inspection with camera (8 angles), status transitions, request approval, upload progress photos, issue invoice, wallet/payouts, offline queue (drift) for status/media. Verify: offline → online sync test. ✅ 2026-08-18 (queue = durable JSON file via `PendingActions` port; drift deferred to backlog)
- **Step 15 — Admin back-office MVP (apps/admin-web).** Per docs/06-DASHBOARDS.md §A: auth (platform roles + 2FA), Overview KPIs (from materialized views), org list/profile, KYB queue (approve/reject with reason), integrations monitor (`integration_requests`, `webhook_events`, DLQ retry, "pending notes" screen), payments/escrow/payouts/ledger views (freeze/release with reason, maker/checker on refunds), disputes room, `platform_settings` editor, audit log browser with export. All sensitive actions require a reason and write `audit_log`. Verify: builds; Playwright smoke (login → KYB approve → retry integration → dispute decision). ✅ 2026-08-19 (disputes room is a placeholder until Step 16; maker/checker on refunds is a reason-gated single step for now — see docs/backlog.md)
- **Step 16 — Disputes module.** Open dispute → freeze escrow, evidence, messages, ops resolution → ledger split/refund; reviews after close. Verify: e2e. ✅ 2026-08-19
- **Step 17 — CI/CD + observability.** GitHub Actions (lint/test/build for api, admin, mobile analyze/test), Docker images, Helm chart skeleton, OpenTelemetry wiring, dashboards JSON. Verify: green pipeline on a clean clone. ✅ 2026-08-19 (every job's commands verified locally; images build only in CI — Docker Hub is unreachable from the dev machine; see docs/runbooks/ci-cd.md)

### Phase 3 — Marketplace & compliance
- **Step 18 — Parts marketplace.** part categories, supplier inventory, part requests with PostGIS matching worker (`part_request_recipients`), bids (upsert), bidding window expiry job, accept → part order → escrow → confirm/auto-confirm, digital warranty issuance (QR token, certificate PDF), warranty claims. Realtime channel `part-request:{id}`. Verify: e2e reverse auction with 3 mock suppliers. ✅ 2026-08-19 (done before Steps 15–17 by owner's product priority)
- **Step 18b — Parts distributors hub.** `part_brands`/`parts_catalog`/`part_fitments` CRUD + CSV import, `InventorySyncPort` (CSV + generic REST adapters, mock) → `supplier_inventory` upsert by `external_sku` with `inventory_sync_runs`, `GET /parts/fit?vin=` (VIN decode → fitments → live offers sorted by price/distance/ETA), Buy Now `part_orders(source=catalog_buy_now)` with `part_order_items`, `trade_accounts` (request → approve → Nafath-signed agreement → credit check on every deferred order → promissory note via Step 10 engine → pay → close), `part_serials` issue/verify/install endpoints + duplicate-scan alert, warranties `part_and_labor`, `group_buys` (join → reached → orders). Verify: e2e "distributor imports CSV → workshop finds part by VIN → buys deferred → note issued → scans QR at install → pays → note closed"; credit-limit rejection test; duplicate-serial alert test. ✅ 2026-08-19 (group buys minimal: open/join/reached; fulfilment orders in backlog; trade-account Nafath agreement signature pending live contract)
- **Step 19 — Logistics.** Transport jobs, driver profiles, quote by distance (maps port + mock), assignment, tracking ingestion (rate-limited), proof of delivery (photo + OTP), fee/margin postings. Verify: e2e tow request. ✅ 2026-08-19 (margin is computed and audited on the job; the ledger posting waits for transport payments — see docs/backlog.md)
- **Step 20 — ZATCA Phase 2.** `packages/zatca-ubl` full UBL 2.1 XML, XAdES signing, CSID onboarding flow (compliance → production), clearance/reporting adapter against ZATCA sandbox, PIH chain per device, submission logging. Verify: passes ZATCA SDK validator on sample invoices. ✅ 2026-08-19 — **verified cryptographically** (signature verifies/tamper fails, OpenSSL accepts the CSR, chain proven in e2e); the ZATCA SDK validator and the sandbox endpoints were **not** exercised from the dev machine — see docs/integrations/zatca.md “before going live”.
- **Step 21 — Accident reports (Monjez/Taqdeer) port + mock** and linkage to work orders. Verify: unit. ✅ 2026-08-20
- **Step 22 — Partner app: scrapyard/parts dealer/distributor & driver flows** (incoming requests, bid with media, orders, warranty issue; distributor: inventory import status, trade-account approvals & aging, serial batches; workshop: VIN parts search, Buy Now cart, trade-account balance, QR install scan; driver job list + tracking). ✅ 2026-08-19 (parts flows; driver flows wait for Step 19; bid photos + warranty issue UI in backlog)
- **Step 23 — Customer app: parts request, bids compare, warranty wallet, tow request.** ✅ 2026-08-20

### Phase 4 — Pilot hardening
- **Step 24 — Security review, pen-test fixes, load test (k6) on WO + bidding, backup/PITR drills, runbooks in `docs/runbooks/`.** ✅ 2026-08-20
- **Step 25 — Pilot config: industrial-zone tagging, onboarding scripts, analytics events, feature flags.** ✅ 2026-08-20

### Phase 5 — Scale features
- **Step 26 — Fleet Hub** (policies, approvals, bulk vehicle import, monthly statements, reports export). ✅ 2026-08-20
- **Step 27 — Voice-to-Invoice** (record → speech port → Claude extraction to line items → review UI). ✅ 2026-08-20 (API + extraction; the recording screen needs a Flutter audio package — backlog)
- **Step 28 — AI Inspection** (vision port → damages JSON on inspections; check-in vs check-out diff). ✅ 2026-08-20
- **Step 29 — Abandoned vehicle flow** (notice schedule, storage fees, enforcement bundle with `is_abandoned_vehicle`). ✅ 2026-08-20
- **Step 30 — Multi-city rollout, Meilisearch discovery, performance & cost tuning.** ✅ 2026-08-21 (multi-city = zone/city config, no code; Meili live-proven; perf = k6 regression pass — remaining cost items in backlog)

## 9. Working agreement with Claude Code

1. Start each session by reading this file and the doc relevant to the step. State which step you are executing.
2. Before writing code for a step, list the files you will create/modify. Keep to the step's scope.
3. Write tests alongside code; run the step's "Verify" commands and paste real output in your summary. If something fails, say so.
4. Never modify append-only tables, ledger invariants, or security guards to make a test pass.
5. When an external contract (Nafath/Nafez/ZATCA/PSP) is unknown, implement the port + mock, document the assumption in `docs/integrations/`, and flag it — do not invent a live API.
6. Update `docs/db/schema.sql` and Prisma together; add an ADR for any architectural change; append `✅` to the completed step in §8.
7. Arabic-first: every user-facing string must have an `ar` value; RTL layouts must be verified.
