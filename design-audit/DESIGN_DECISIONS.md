# قرارات التصميم — وسببها

| # | القرار | السبب | البديل المرفوض |
|---|---|---|---|
| D1 | **إعادة تصميم داخل النظام الحالي** لا إعادة كتابة | ٢١٢ اختباراً و٤٨ شاشة تعمل ومربوطة؛ الهوية v2 معتمدة؛ الخطر في إعادة الكتابة أكبر من الفائدة | مشروع Flutter جديد |
| D2 | **الوضع الداكن يبقى مقفلاً**، ورموزه تُعزل | قرار المالك ٢٩ أغسطس؛ صيانة لقطات داكنة لوضعٍ مقفل تبديد | إعادة تفعيله |
| D3 | **بطاقة ختم واحدة لكل شاشة** | حين يكون كل شيء بطلاً لا بطل؛ الختم يفقد معناه | إبقاؤها كبطاقة عامة |
| D4 | **حذف شبكة الخدمات الست** لصالح صفّ ثلاثي + «المزيد» | الحلقة (إصلاح · قطعة · سطحة) هي المنتج؛ البطاقات المتطابقة نمط AI عام | إبقاء ٦ بطاقات بألوان مختلفة |
| D5 | **الخطأ لا يهرب**: `showFailure` موحّد (٣٩ موضعاً) — شريط خطأ بلون الحالة وأيقونتها، ٨ ثوانٍ، زرّ إغلاق، فوق شريط التنقّل لا خلفه؛ و`InlineError` حيث يمكن الإبقاء عليه في مكانه | كان ٤ ثوانٍ بلا لون خلف الشريط؛ MaterialBanner بمؤقّتٍ خارجي يترك Timer معلّقاً في الاختبارات | MaterialBanner دائم (يلتصق بالشاشة التالية) |
| D6 | **التحميل هيكلي لا مُعتِم** (`AsyncResultView` يعرض هيكل الشاشة) | الميثاق: «التحميل لا يحجب الشاشة» | دوّار في المنتصف |
| D7 | **الإشعارات جرسٌ في الرأس** لا خلف «…» | «إشعار مستمر مثل التوصيل» توجيه المالك | إبقاؤها في القائمة |
| D8 | **الحالة بصوت الطرف** (`Labels.driverStatus` نموذجاً) تُعمَّم | رُصد على الجهاز؛ الثقة تُبنى من الجملة الصحيحة | نص واحد للجميع |
| D9 | **لا حركة مخصّصة** | ميثاق §5.0/6؛ M3 يعطي fade-forwards | انتقالات hero |
| D10 | **الأوراق بحدٍّ لا ظلّ**، ظلٌّ واحد للبطل | الظلال المتراكمة تُطفئ الصفحة وتساوي بين العناصر | ظلّ لكل بطاقة |
| D11 | **الشريك ٤ تبويبات**؛ المساعد زرّ داخل «اليوم» | الميثاق؛ ٧ مداخل في شاشة البداية كثيرة | تبويب خامس للمساعد |
| D12 | **«مبيعاتي» بشرائح ثلاث** | قائمة واحدة تخلط ثلاثة مواضيع؛ زرّ الملصقات كان على بُعد ٤ تمريرات | تبويب رابع |
| D13 | **الاعتماد: الإجمالي أولاً ثم «ماذا يعني توقيعي» ثم البنود** | لحظة قانونية؛ GOV.UK: ابدأ بما يحتاجه المستخدم | عرض اللقطة كما هي |
| D14 | **اللوحة: preset واحد + ٤ مجموعات قائمة** | هوية غائبة؛ ١٦ صفحة مسطّحة | إعادة كتابة اللوحة |
| D15 | **الأرقام غربية + tabular** في كل مبلغ ومرجع | قرار سابق (Fmt) يُعمَّم | أرقام عربية-هندية |
| D16 | **القياس على الجهاز** قبل ✅ | ١٥ عطلاً مرئياً عاشت فوق اختبارات خضراء | الاعتماد على اللقطات الذهبية |
| D17 | **حذف محلّل «قل وش فيها» (AI) بالكامل** — لا شاشة، لا زرّ ميكروفون في شريط التنقّل، لا مسار `/ask` | توجيه المالك المباشر ٢٥ سبتمبر: «منتج حقيقي يقدم رحلة للعميل» لا واجهة محادثة موازية للرحلة الفعلية (سيارة → عطل → عروض → أمر موقّع → دفع محفوظ) | إبقاؤه خلف راية أو تصغيره |

## ما لم يُقرَّر وقد يحتاج المالك
- **سطح حاسوب للأسطول** (قسم في لوحة الويب) — خارج هذه الجولة.
- **الاسم في الواجهة الإنجليزية** (Sinaaty/Senaeya) — يبقى «Sinaaty» حتى يُقرَّر.
